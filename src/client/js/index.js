/*
 * pwix:accounts-hub/src/client/js/index.js
 */

import '../../common/js/index.js';

import './functions.js';

import '../stylesheets/accounts_hub.less';

// on client side, the package is now fully evaluated, so ready
AccountsHub.ready( true );
